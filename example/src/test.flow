func closure(a: int) func(int) int {
    var b = a + 2;
    const double = a * 2;
    return func(c: int) use (b, double) int {
        return b + double + c;
    };
}

const function = closure(2);
print(function(1));
