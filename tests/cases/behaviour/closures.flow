func produceMultiplier(factor: int) func(int) int {
    return func(a: int) use (factor) int {
        return a * factor;
    };
}

const doubler = produceMultiplier(2);
const tripler = produceMutliplier(3);

print(doubler(4));
print(tripler(5));

=====
8
15
